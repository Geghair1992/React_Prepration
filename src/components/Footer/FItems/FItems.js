import React from 'react'

const FItems = (props) => {
    return (
        <li>
            <a href="http://google.com">{props.ft}</a>
        </li>
            
        
    );
};

export default FItems
